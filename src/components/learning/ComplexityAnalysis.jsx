const TEXT = {
  conclusion: '复杂度结论',
  best: '最好',
  average: '平均',
  worst: '最坏',
  space: '空间',
  timeReason: '时间怎么来',
  spaceReason: '空间怎么来',
  note: '适用提醒',
}

const DETAILS = {
  bubblesort: {
    time: '外层最多进行 n-1 轮扫描；每轮比较相邻元素并把当前最大值推到未排序区末尾。因此比较次数是 (n-1)+(n-2)+...+1，量级为 O(n²)。如果加入“本轮无交换就提前结束”的优化，已有序数组只需要一轮扫描，所以最好为 O(n)。',
    space: '只在交换相邻元素时使用常数个临时变量，不需要额外数组。',
    note: '冒泡排序适合教学和小规模数据；真实工程里通常不会作为通用排序方案。',
  },
  selectionsort: {
    time: '第 i 轮都要在剩余区间里找最小值，无论数组是否已有序都要完整扫描。因此比较次数固定为 (n-1)+(n-2)+...+1，最好、平均、最坏都是 O(n²)。',
    space: '只记录当前最小值下标并做原地交换，额外空间为 O(1)。',
    note: '它的交换次数最多 n-1 次，适合“写入代价高、比较代价低”的场景。',
  },
  insertionsort: {
    time: '每个元素向左寻找插入位置。已有序时每个元素只比较一次，最好 O(n)；逆序时第 i 个元素要移动 i 次，总移动量为 1+2+...+(n-1)，最坏 O(n²)。',
    space: '插入过程只保存当前 key 和少量指针，额外空间为 O(1)。',
    note: '插入排序对小数组和接近有序的数据很高效，常被用作混合排序的小规模分支。',
  },
  shellsort: {
    time: '希尔排序把插入排序扩展为多轮 gap 插入。大 gap 先让元素快速接近正确区域，小 gap 再做精修。复杂度取决于 gap 序列，常见实现平均优于 O(n²)，但没有统一的精确表达。',
    space: '每轮仍是原地插入排序，只需要常数级临时变量。',
    note: '它适合内存紧张且希望比普通插入排序更快的场景，但稳定性通常无法保证。',
  },
  quicksort: {
    time: '每次 partition 线性扫描当前区间。如果 pivot 能较均匀地切分数组，递归深度约 log n，总时间为 O(n log n)。如果每次都切出 0 和 n-1 的极端划分，递归深度退化为 n，总时间为 O(n²)。',
    space: '主要空间来自递归调用栈。平均递归深度为 O(log n)，最坏可到 O(n)。',
    note: '随机 pivot、三数取中和小数组切换插入排序都能显著降低退化概率。',
  },
  mergesort: {
    time: '每层递归都要合并所有元素一次，单层工作量 O(n)；数组每次二分，层数为 O(log n)，所以总时间稳定为 O(n log n)。',
    space: '合并两个有序区间通常需要一个临时数组保存结果，额外空间为 O(n)。',
    note: '归并排序稳定且最坏情况有保证，很适合链表排序和外部排序。',
  },
  heapsort: {
    time: '建堆可以自底向上在线性时间 O(n) 完成；随后执行 n-1 次取堆顶，每次下沉调整为 O(log n)，所以排序阶段为 O(n log n)。',
    space: '堆直接放在原数组里，只需要少量临时变量，额外空间为 O(1)。',
    note: '堆排序最坏情况稳定为 O(n log n)，但缓存局部性通常不如快速排序。',
  },
  countingsort: {
    time: '先扫描 n 个元素计数，再扫描值域 k 做前缀和，最后再扫描 n 个元素回填，因此时间为 O(n+k)。',
    space: '需要计数数组和输出数组，空间为 O(n+k)。',
    note: '只有当值域 k 不大且元素能映射到整数桶时，计数排序才划算。',
  },
  radixsort: {
    time: '按位处理 k 轮，每轮用稳定的线性排序处理 n 个元素，因此总时间为 O(nk)。',
    space: '每一轮通常需要计数桶和临时输出数组，空间为 O(n+k)。',
    note: '关键点是每一轮必须稳定，否则低位已经建立的顺序会被破坏。',
  },
  bucketsort: {
    time: '先把 n 个元素分配到 k 个桶，再分别排序桶内元素。数据均匀分布时桶内很小，平均接近 O(n+k)；如果所有元素落入同一个桶，会退化到桶内排序的最坏情况。',
    space: '需要为桶和桶内元素保存额外结构，通常为 O(n+k)。',
    note: '桶排序强依赖输入分布，适合已知范围且近似均匀的数据。',
  },
  bfs: {
    time: '每个顶点最多入队出队一次，每条边最多在遍历邻接表时检查一次，所以总时间为 O(V+E)。',
    space: 'visited 集合和队列最多保存 O(V) 个顶点。',
    note: 'BFS 求的是无权图或等权图中的最少边数路径。',
  },
  dfs: {
    time: '每个顶点访问一次，每条边检查一次，因此邻接表表示下时间为 O(V+E)。',
    space: '递归栈或显式栈的深度最坏可到 O(V)，visited 也需要 O(V)。',
    note: 'DFS 常用于连通性、拓扑排序、环检测和回溯搜索。',
  },
  dijkstra: {
    time: '使用优先队列时，每条边可能触发一次松弛和入队，堆操作为 O(log V)，整体为 O((V+E)log V)。朴素数组实现则通常为 O(V²)。',
    space: '距离数组、访问标记和优先队列都按顶点或边的规模增长。',
    note: 'Dijkstra 要求边权非负；存在负权边时应考虑 Bellman-Ford。',
  },
  bellmanford: {
    time: '最短路径最多包含 V-1 条边，因此需要做 V-1 轮松弛；每轮扫描全部 E 条边，总时间为 O(VE)。',
    space: '主要保存 dist 数组，空间为 O(V)。',
    note: '额外做第 V 轮松弛可以检测负权环。',
  },
  floydwarshall: {
    time: '三重循环枚举中转点 k、起点 i、终点 j，每组状态做一次松弛，因此时间为 O(V³)。',
    space: '需要一张 V×V 的距离矩阵，空间为 O(V²)。',
    note: '适合顶点数不大、需要全源最短路的场景。',
  },
  toposort: {
    time: '计算入度需要扫描所有顶点和边；之后每个顶点出队一次，每条边只导致一次入度减少，总时间为 O(V+E)。',
    space: '入度数组、队列和结果序列都需要 O(V)。',
    note: '如果最终输出顶点数小于 V，说明图中存在环。',
  },
  prim: {
    time: '优先队列版本每次选出连接生成树的最小边，并松弛邻边，复杂度通常为 O(E log V)。',
    space: '需要邻接表、访问标记和优先队列，辅助空间与 V、E 相关。',
    note: 'Prim 更适合从某个起点逐步扩张生成树。',
  },
  kruskal: {
    time: '瓶颈在边排序，E 条边排序为 O(E log E)；并查集合并和查询近似常数，整体仍由排序主导。',
    space: '需要保存边列表和并查集 parent/rank 数组。',
    note: 'Kruskal 更适合稀疏图，思路是按边权从小到大尝试加入。',
  },
  bst: {
    time: '查找、插入、删除都沿树高走一条路径。树平衡时高度 O(log n)，退化成链表时高度 O(n)。',
    space: '存储 n 个节点需要 O(n)；递归操作还可能使用 O(h) 调用栈。',
    note: '普通 BST 不保证平衡，输入有序时容易退化。',
  },
  redblack: {
    time: '红黑树通过颜色约束保证树高为 O(log n)，因此查找、插入、删除的路径长度都有对数上界。',
    space: '每个节点保存键值、指针和颜色位，总体空间 O(n)。',
    note: '红黑树牺牲一点严格平衡性，换取较少旋转和稳定的最坏情况表现。',
  },
  avl: {
    time: 'AVL 树保持任意节点左右子树高度差不超过 1，因此树高为 O(log n)，查找、插入、删除都为 O(log n)。',
    space: '保存 n 个节点和每个节点高度信息，空间 O(n)。',
    note: 'AVL 比红黑树更严格平衡，查询快，但更新时旋转可能更频繁。',
  },
  treap: {
    time: 'Treap 用随机优先级把 BST 随机化，期望树高为 O(log n)，所以操作期望为 O(log n)。最坏情况下仍可能退化到 O(n)。',
    space: '每个节点保存 key、priority 和左右指针，空间 O(n)。',
    note: 'Treap 实现简单，但性能保证是概率意义上的。',
  },
  knapsack: {
    time: '状态由“前 i 个物品”和“容量 w”共同决定，共有 n×W 个状态，每个状态 O(1) 转移，所以时间为 O(nW)。',
    space: '二维 DP 为 O(nW)；如果只依赖上一行，可以滚动压缩到 O(W)。',
    note: '这里的 W 是容量数值，不是输入位数，因此这是伪多项式复杂度。',
  },
  lcs: {
    time: 'dp[i][j] 表示两个前缀的 LCS 长度，共有 m×n 个状态，每个状态只比较末尾字符并取最大值，时间为 O(mn)。',
    space: '完整表为 O(mn)；只求长度时可压缩为 O(min(m,n))。',
    note: '如果要还原具体子序列，通常需要保留路径或完整 DP 表。',
  },
  lis: {
    time: '基础 DP 会对每个 i 枚举所有 j<i，因此是 O(n²)。二分优化维护 tails 数组，每个元素二分定位，时间为 O(n log n)。',
    space: 'DP 或 tails 数组都需要 O(n) 空间。',
    note: 'tails[i] 不是实际序列，而是长度为 i+1 的递增子序列的最小可能结尾。',
  },
  editdistance: {
    time: 'dp[i][j] 表示把 s1 前 i 个字符变成 s2 前 j 个字符的最少操作数，共有 mn 个状态，每个状态从插入、删除、替换三种来源取最小值。',
    space: '完整 DP 表为 O(mn)；只求距离时可以保留两行，压缩到 O(min(m,n))。',
    note: '编辑距离适合衡量两个字符串的相似程度。',
  },
  coinchange: {
    time: '对每种硬币遍历目标金额区间，每个状态尝试用当前硬币更新最优值，所以时间为 O(nW)。',
    space: '一维 dp[0..W] 保存每个金额的最优解，空间为 O(W)。',
    note: '完全背包要正序遍历金额，才能允许同一硬币被重复使用。',
  },
  fifo: {
    time: '每次页面访问只做哈希查询、队列入队或出队，单次 O(1)，处理 n 次访问为 O(n)。',
    space: '内存中最多保存 capacity 个页面，空间为 O(capacity)。',
    note: 'FIFO 简单但可能出现 Belady 异常。',
  },
  lru: {
    time: '哈希表负责 O(1) 定位页面，双向链表负责 O(1) 移动最近使用项，因此单次访问均摊 O(1)。',
    space: '哈希表和链表最多保存 capacity 个页面。',
    note: '如果只用数组或普通列表维护最近使用顺序，单次移动会退化为 O(capacity)。',
  },
  opt: {
    time: 'OPT 需要知道未来访问序列。朴素实现每次缺页都向后扫描，找最晚再次访问的页面，代价较高。',
    space: '需要保存当前页框以及未来访问序列，辅助空间与页框数和访问序列相关。',
    note: 'OPT 是理论最优基准，通常用于离线比较而不是在线系统。',
  },
  diskfcfs: {
    time: '按照请求到达顺序逐个服务，每个请求计算一次磁头移动距离，总时间为 O(n)。',
    space: '需要保存请求队列，空间为 O(n)。',
    note: 'FCFS 公平但可能产生很长的磁头移动距离。',
  },
  sstf: {
    time: '每次从未服务请求中找距离当前磁头最近的请求。朴素扫描一次 O(n)，重复 n 次就是 O(n²)。',
    space: '保存未处理请求列表，空间为 O(n)。',
    note: 'SSTF 能减少平均寻道距离，但远端请求可能长期等待。',
  },
  scan: {
    time: '通常先按磁道号排序 O(n log n)，之后磁头像电梯一样单向扫描并反向，总扫描 O(n)。',
    space: '排序和请求列表需要 O(n) 空间。',
    note: 'SCAN 比 SSTF 更公平，能缓解饥饿问题。',
  },
  naive: {
    time: '对每个可能起点尝试匹配模式串。最坏情况下每个起点都比较 m 个字符，共 O(nm)。',
    space: '只使用两个下标指针，空间为 O(1)。',
    note: '模式串很短或文本很小时，朴素匹配简单直接。',
  },
  kmp: {
    time: '预处理 LPS 数组 O(m)；匹配时主串指针不回退，每个字符最多被处理常数次，因此总时间 O(n+m)。',
    space: 'LPS 数组长度为 m，空间 O(m)。',
    note: 'KMP 的核心收益是失配时复用已匹配前缀信息。',
  },
  rabinkarp: {
    time: '滚动哈希让每个窗口的哈希更新为 O(1)，平均只需线性扫描。若大量哈希碰撞，每次都要逐字符验证，最坏可到 O(nm)。',
    space: '只维护哈希值和少量常量，空间 O(1)。',
    note: 'Rabin-Karp 适合多模式匹配或需要快速筛选候选窗口的场景。',
  },
  nqueens: {
    time: '每行尝试放置一个皇后，并用列和对角线约束剪枝。理论上搜索树规模接近排列数量，最坏常记为 O(n!)。',
    space: '递归深度为 n，并保存列、主对角线、副对角线占用状态，空间 O(n)。',
    note: '剪枝能显著减少实际搜索量，但问题本质仍是指数级搜索。',
  },
  unionfind: {
    time: '路径压缩和按秩合并一起使用后，单次 find/union 的均摊复杂度为 O(α(n))，实际几乎可视为常数。',
    space: 'parent、rank/size 数组都按元素个数增长，空间 O(n)。',
    note: '并查集适合处理动态连通性，但不擅长支持删除边。',
  },
  trie: {
    time: '插入和查找只沿字符串长度 m 逐字符向下走，和字典中已有单词数基本无关，因此为 O(m)。',
    space: '最坏情况下每个字符都创建一个新节点，总空间与所有字符串总长度成正比。',
    note: 'Trie 在前缀查询上优势明显，但节点指针会带来额外内存开销。',
  },
  linkedlist: {
    time: '按位置访问需要从头遍历 O(n)；如果已知节点位置，插入和删除只改指针，为 O(1)。',
    space: '每个节点除数据外还保存指针，总空间 O(n)。',
    note: '链表适合频繁插入删除，不适合随机访问。',
  },
  astar: {
    time: '复杂度取决于启发函数质量。启发越接近真实代价，展开节点越少；h=0 时退化为 Dijkstra。',
    space: 'open 集、closed 集和路径信息都可能保存大量节点，最坏与搜索空间规模同阶。',
    note: '可采纳启发函数不会高估真实代价，是保证最优性的关键。',
  },
  hashtable: {
    time: '理想哈希下，插入、查找、删除只需定位桶并处理很短的冲突链，平均 O(1)。如果大量键落入同一桶，最坏退化为 O(n)。',
    space: '桶数组和元素存储都随元素数量增长，空间 O(n)。',
    note: '控制负载因子和选择好的哈希函数，是维持平均 O(1) 的关键。',
  },
  segtree: {
    time: '线段树高度为 O(log n)。区间查询和单点更新只访问少量跨层节点，因此为 O(log n)；建树会访问每个节点一次，为 O(n)。',
    space: '数组实现通常开 4n 规模的树空间，量级为 O(n)。',
    note: '需要区间更新时可以加入懒标记，把区间更新也保持在 O(log n)。',
  },
}

const CATEGORY_FALLBACKS = {
  sorting: {
    time: '排序算法的时间主要来自比较、移动或按桶分配元素。关注是否有嵌套扫描、是否每层都处理全部元素，以及是否依赖值域大小。',
    space: '空间取决于是否原地交换、是否需要临时数组或桶结构。',
    note: '同为排序算法，稳定性、原地性和输入分布会显著影响选择。',
  },
  graph: {
    time: '图算法通常按顶点数 V 和边数 E 分析。遍历类算法多为 O(V+E)，带优先队列或全源状态时复杂度会更高。',
    space: '通常需要 visited、dist、parent 等数组，空间至少与顶点数相关。',
    note: '图的稀疏或稠密程度会影响最合适的数据结构和算法。',
  },
  dp: {
    time: '动态规划复杂度由状态数量乘以单个状态转移代价决定。',
    space: '空间由 DP 表大小决定，若只依赖相邻行或相邻状态，通常可以滚动压缩。',
    note: '先明确状态定义，再看状态维度，是分析 DP 复杂度的关键。',
  },
}

export default function ComplexityAnalysis({ algo }) {
  const detail = DETAILS[algo.slug] || CATEGORY_FALLBACKS[algo.category] || {
    time: '从算法执行流程出发，统计核心循环、递归层数或状态数量，即可得到时间复杂度。',
    space: '额外空间主要来自辅助数组、递归栈、队列、哈希表或图结构。',
    note: '复杂度只是增长趋势，实际性能还会受到常数、缓存、输入分布和实现方式影响。',
  }

  return (
    <div style={{
      display: 'grid',
      gap: 14,
    }}>
      <div style={{
        display: 'grid',
        gridTemplateColumns: 'repeat(auto-fit, minmax(240px, 1fr))',
        gap: 12,
      }}>
        <ReasonBlock title={TEXT.timeReason} text={detail.time} />
        <ReasonBlock title={TEXT.spaceReason} text={detail.space} />
        <ReasonBlock title={TEXT.note} text={detail.note} />
      </div>
    </div>
  )
}

function ReasonBlock({ title, text }) {
  return (
    <div style={{
      padding: '16px 18px',
      borderRadius: 12,
      background: 'var(--glass-bg)',
      border: '1px solid var(--glass-border)',
      boxShadow: 'var(--glass-shine)',
    }}>
      <h3 style={{
        fontSize: 13,
        margin: '0 0 8px',
        color: 'var(--text-primary)',
        fontWeight: 800,
      }}>{title}</h3>
      <p style={{
        margin: 0,
        color: 'var(--text-secondary)',
        fontSize: 13.5,
        lineHeight: 1.8,
      }}>{text}</p>
    </div>
  )
}

